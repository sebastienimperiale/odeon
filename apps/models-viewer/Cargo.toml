[package]
name = "odeon-models-viewer"
description = "The models-only desktop viewer: forward models, their parameters, trajectories and animated scenes, without the estimators"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[[bin]]
name = "models-viewer"
path = "src/main.rs"

[dependencies]
ode-models-egui = { path = "../../crates/ode-models-egui" }
eframe          = "0.35"
